# LOCAL mode configuration
# Zero-cost, external APIs blocked, device-adaptive model selection

safety:
  allow_external_apis: false

training:
  batch_size: 2
  group_size: 2

generation:
  max_new_tokens: 320

judge:
  provider: "local"
  model: "Qwen/Qwen2-VL-2B-Instruct"
